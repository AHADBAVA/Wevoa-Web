# Dashboard Demo

Install the official packages, then run:

```text
wevoa install auth
wevoa install db
wevoa install utils
wevoa start
```

This demo shows a protected dashboard backed by `@auth` and `@db`.
