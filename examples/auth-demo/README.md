# Auth Demo

Install the official packages, then run the demo:

```text
wevoa install auth
wevoa install db
wevoa install utils
wevoa start
```

Routes:

- `/` login + registration landing page
- `/account` protected account page
- `/logout` session sign-out
